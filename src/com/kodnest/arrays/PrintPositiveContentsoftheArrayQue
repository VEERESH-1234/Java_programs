Write a java program to take array length as input from user and create an integer array and
store elements into the array by taking elements as input from keyboard.Print the array
contents on to console if console if array content is positive.

Note:Use Enhanced for loop


Explanation:
Asking the user to enter the length of an array.
we have to create an int array.
Asking the user to enter the elements of the array.
Storing the elements in the array by using the for loop.
Displaying the elements of the array if the elements is positive by using the loop.